{
  "name": "hikaricp",
  "version": "1.0.0",
  "description": "Static GitHub Pages site for the HikariCP explorer",
  "private": true,
  "scripts": {
    "build:pages": "rm -rf dist && mkdir -p dist && cp index.html styles.css dist/ && rm -rf dist/image dist/js && cp -R image dist/ && cp -R js dist/ && touch dist/.nojekyll",
    "serve": "python3 -m http.server 4173"
  }
}
