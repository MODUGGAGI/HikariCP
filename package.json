{
  "name": "hikaricp",
  "version": "1.0.0",
  "description": "Static GitHub Pages site for the HikariCP explorer",
  "private": true,
  "scripts": {
    "build:pages": "rm -rf dist && mkdir -p dist && cp -R app/. dist/ && touch dist/.nojekyll",
    "serve": "python3 -m http.server 4173 -d app"
  }
}
